# Markdown Project

A simple project to store markdown documentation files.

## Structure

- `docs/` - Directory containing markdown documentation files

## Usage

Store all your markdown files in the `docs/` directory.
